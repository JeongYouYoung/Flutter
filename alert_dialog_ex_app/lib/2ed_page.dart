
import 'package:flutter/material.dart';

class Page2ed extends StatelessWidget {
  const Page2ed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Second Page"),
      ),
    );
  }
}